// Whereas MessagesView controls the entire list of messages,
// MessageView is responsible for rendering a single message.

var MessageView = {
  // Learn more about Underscore's templating capability
  // here: https://underscorejs.org/#template.
  // TODO: Update this template accordingly.
  // render: _.template(

  //   "<div class='singleMessageContainer'>" +
  //     `<div class=\'username\'>" +
  //     ${this.message.username}
  //     "</div>` +
  //     `<div class=\'singleMessage\'>${this.message.text}</div>` +
  //     `<div class='roomName'><%=message.roomName></div>` +
  //   '</div>'


  // //   "<div class='module module-movie' style='background-image: url(<%= movieImage %> '>" +
  // //   "<div class='movie-info'>" +
  // //     "<h3 class='movie-title'>" +
  // //        "<%= movieTitle %>" +
  // //     "</h3>" +
  // //     "<p class='movie-director'>" +
  // //        "<%= movieDirector %>" +
  // //     "</p>" +
  // //   "</div>" +
  // // "</div>"

  // )

};


// MessagesView.$chats.append( '<div id="singleMessageContainer"></div>' );

// $("#singleMessageContainer").append(`<div id='singleMessage'>Username: ${message.username} `);

// MessagesView.$chats.append( `<div id='singleMessage'>Username: ${message.username} <br> Message: ${message.text} <br> Roomname: ${message.roomName} <br> -----------------------</div>` );

// MessagesView.$chats.append( `<div id='singleMessage'>Username: ${message.username} <br> Message: ${message.text} <br> Roomname: ${message.roomName} <br> -----------------------</div>` );


// var i;
// var html = '';
// for (i = 0; i < data.movies.length; i++) {
//   html += compiled(data.movies[i]);
// }
// $('#movies').append(html);